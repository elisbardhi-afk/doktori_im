// jest-dom matchers are only meaningful in a DOM environment. Loading them
// unconditionally pulls jsdom into pure-node tests, so guard on the env.
import "@testing-library/jest-dom/vitest";

export {};
